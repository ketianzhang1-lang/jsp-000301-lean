# Verification status

Prepared 2026-09-17 UTC. The submitted proof and scripts are ready for the pinned CI run. Final machine-check results and the immutable tested commit will be recorded after the run. No successful independent check is claimed by this provisional receipt.

The original upstream source is retained for comparison. The tested main module must be JSP000897 and both classical endpoints and all surplus endpoints must pass the eight-target Audit.lean check.

The CI uses official compiled Mathlib caches. Leanchecker is Lean's own kernel; NaNoda is the separate Rust implementation. This is contributor-run reproduction, not official verification, an offline dependency rebuild, independent human sign-off or award approval. GitHub artifacts have finite retention; their expiry and GitHub-reported digest will be recorded after the run.
